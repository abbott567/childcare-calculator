'use-strict';

const {expect} = require('chai');

// Toddler 42.50
// Toddler 23.25

describe('full time childcare', () => {
  it('should return 255 per week', () => {
    expect(true).to.eql(true);
  });
});
